/**
 * Public base URL of the site. Placeholder — replace with the real domain
 * when it is known. Imported by both the app (SEO tags) and
 * scripts/postbuild.ts (sitemap/rss/robots), so it lives in exactly one place.
 */
export const SITE_URL = 'https://pingboard.dev'

export const SITE_NAME = 'PingBoard'

export const SITE_DESCRIPTION =
  'PingBoard is a single-binary uptime monitor with built-in status pages. HTTP, TCP, ping, DNS, keyword & JSON checks — Discord, Slack, webhook, email, ntfy alerts.'
